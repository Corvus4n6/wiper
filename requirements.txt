# OWL - Optimized Wipe and Logging
# Python package requirements for wiper.py
#
# Install with:
#   pip install -r requirements.txt
#
# System dependencies (install via package manager, not pip):
#   hdparm        -- required for --hw-erase / --hw-secure on ATA/SATA drives
#                    Debian/Ubuntu: apt install hdparm
#   nvme-cli      -- required for --hw-erase / --hw-secure on NVMe drives
#                    Debian/Ubuntu: apt install nvme-cli
#   smartmontools -- required for SMART data capture in --report
#                    Debian/Ubuntu: apt install smartmontools
#
# Python 3.10+ required.

blkinfo>=0.1.3
pypdf>=5.9.0
reportlab>=3.6.12
rich>=13.0.0
